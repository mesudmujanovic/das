import { AddNumLetterToDivsDirective } from './add-num-letter-to-divs.directive';

describe('AddNumLetterToDivsDirective', () => {
  it('should create an instance', () => {
    const directive = new AddNumLetterToDivsDirective();
    expect(directive).toBeTruthy();
  });
});
